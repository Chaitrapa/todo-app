name: ToDo CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Build
      run: echo "Building ToDo App..."

    - name: Test
      run: echo "Testing ToDo App..."

    - name: Deploy
      run: echo "Deploying ToDo App..."
